'use strict';

/**
 * gamification-tx-type router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::gamification-tx-type.gamification-tx-type');
