'use strict';

/**
 * gamification-tx-type service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::gamification-tx-type.gamification-tx-type');
