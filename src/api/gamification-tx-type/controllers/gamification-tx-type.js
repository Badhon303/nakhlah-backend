'use strict';

/**
 * gamification-tx-type controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::gamification-tx-type.gamification-tx-type');
