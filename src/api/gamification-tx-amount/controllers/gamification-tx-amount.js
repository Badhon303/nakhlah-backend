'use strict';

/**
 * gamification-tx-amount controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::gamification-tx-amount.gamification-tx-amount');
