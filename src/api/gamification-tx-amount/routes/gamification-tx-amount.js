'use strict';

/**
 * gamification-tx-amount router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::gamification-tx-amount.gamification-tx-amount');
