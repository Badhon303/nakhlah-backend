'use strict';

/**
 * gamification-tx-amount service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::gamification-tx-amount.gamification-tx-amount');
