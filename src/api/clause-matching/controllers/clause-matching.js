'use strict';

/**
 * clause-matching controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::clause-matching.clause-matching');
